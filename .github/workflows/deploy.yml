name: Deploy to GitHub Pages

on:
  push:
    branches: [main, claude/phase-1-games-pwa-22picd]
  workflow_dispatch:

# Pages needs these; nothing here can write to the repository.
permissions:
  contents: read
  pages: write
  id-token: write

# Let a running deploy finish rather than cancelling it half way.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci

      # The gate that matters: a wrong word reaching a child is the worst
      # thing this app can do, so nothing ships until the content test passes.
      - name: Content
        run: npm run validate:content

      - name: Build
        run: npm run build

      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
